<html>
 <head></head>
 
 <body>
  <div class="bg-mask">
   <div class="title">Rails <span>Que</span>stions</div>
   
   <div class="top_info">
   <%=link_to "採用担当者様へ", "/questions", method: :get  %>
   </div>
   
   <h1>問題一覧</h1>
   ＊入力エリアの右下をドラックすると任意の大きさに変化できます。
   <div class="qsmenu">
    <%= form_for(@question) do |f| %>
      <div class="qs_make">クイズタイトル</qs_make><br>
      <!--text_areaは改行挟む長い文に適している-->
      <%= f.text_area :title, placeholder: "例：railsコマンド" %><br>
      <div class="qs_make">問題</qs_make><br>
      <%= f.text_area :questions, placeholder: "例：サーバーを立ち上げるコマンドは？" %><br>
      <div class="qs_make">選択肢A</qs_make><br>
      <%= f.text_area :choice1, placeholder: "例：rails routes" %><br>
      <div class="qs_make">選択肢B</qs_make><br>
      <%= f.text_area :choice2, placeholder: "例：rails g" %><br>
      <div class="qs_make">選択肢C</qs_make><br>
      <%= f.text_area :choice3, placeholder: "例：rails s" %><br>
      <div class="qs_make">正解</qs_make><br>
      <%= f.text_area :correct, placeholder: "例：rails s" %><br>
      <%= f.submit "問題を作る" %><br>
    <% end %>
    
    <% @questions.each do |question| %>
      <%= question.title %>
      <%=link_to "問題へ", "/questions/#{question.id}/edit", method: :get  %>
      <%=link_to "削除", "/questions/#{question.id}", method: :delete  %><br>
    <% end %>
    
    試作品ですので問題数は少ないです<br>
   </div>
  </div>
 </body>
 
</html>